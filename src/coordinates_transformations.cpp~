Point2f meter_diff_to_pixel_diff(const cv::Point2f& diff,
                                 const cv::Point2d& img_dim) {
  
}


Point2f pixel_diff_to_meter_diff(const cv::Point2f& diff,
                                 const cv::Point2d& img_dim) {
  
}

Point2f pixel_pos_to_ecef_pos(const cv::Point2f& diff,
                                 const cv::Point2d& img_dim) {
  
}
